This task is COMPLETED.
@if (nextTask)
Next task: {{ nextTask.id }}. Engineer and Reviewer should call await_* with taskId: "{{ nextTask.id }}".
@else
The Head Planner must now decide the next move.
@endif

---
### ACTIVE TASK:
Feature ID: {{ task.featureId }}
Task ID: {{ task.taskId }}
Phase: {{ task.phase }}

### TASK SPECIFICATION:
{{ task.spec | json }}

@if (task.handoff)
---
### LATEST HANDOFF ({{ task.handoff.type }}):
{{ task.handoff.data | json }}
@endif
